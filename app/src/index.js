import React              from 'react';
import ReactDOM           from 'react-dom';
import EmployerView       from './view/employer-view.js';

ReactDOM.render(
	<EmployerView/>,
	document.getElementById("root")
);